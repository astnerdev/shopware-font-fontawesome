import './scss/base.scss';

// Ensure the JavaScript entry point exists even if we do not register any
// custom behaviour yet. This keeps the plugin future-proof and compatible
// with Shopware's storefront build process.
import './js/main';
